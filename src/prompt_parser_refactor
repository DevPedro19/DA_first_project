One ideas that would greatly improve the functionality of the parser adn that currently is not supported id the fact
that I'm parsing the CSV as a single file. One better approach could be subdividing the csv once and then, for each
individually parsed csv, one for each section: Submissions, Reviewers, Parameters and Control (new csv name like dataset1_submissions and so on).
After that load each csv: currently I'm doing that line by line to check for invalid fields and interrupt execution
but I could also do it after having the csv loaded and then check each field.
I implemented validation for checking if the parsed value of the field is valid and I want to keep that same functionality.
Also, the overall way that the parser works could be maintained.
However that part that should be changed the most is setting the values of the struct Data.
Do you think this is a good idea. First of all elaborate a plan and see if my idea is good enough to spend time instead
of keeping the code as is since its fully functional.
See if new classes are needed to keep fundamental architectural designs valid like single responsability principle.
Focus on overall simple code without crazy programming language hacks.